platform :ios, '14.0'

inhibit_all_warnings!

target 'MishuApp' do
  use_frameworks! :linkage => :static

  # 微信SDK
  pod 'WechatOpenSDK-XCFramework'

  # 友盟统计 + U-APM（包含崩溃监控）
  pod 'UMCommon'
  pod 'UMDevice'
  pod 'UMAPM'
end
