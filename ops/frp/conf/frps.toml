bindAddr = "0.0.0.0"
bindPort = 7000

auth.method = "token"
auth.token = "tk-bnaojsdabhioahd80"

# 供 nginx 转发 /local 使用
vhostHTTPPort = 18080

# 可选：管理面板（生产建议仅内网放行）
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "gg2228GGF!"

transport.tcpMux = true
log.to = "./frps.log"
log.level = "info"
log.maxDays = 3
